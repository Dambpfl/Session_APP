
{% extends 'base.html.twig' %}

{% block title %}Ajouter un formateur{% endblock %}

{% block body %}
    {% if edit %}
        <h1>Modifier un formateur</h1>     
    {% else %}
        <h1>Ajouter un formateur</h1>
    {% endif %}

    <p>{{ form(formAddFormateur) }}</p>


{% endblock %}